/* TechSeekho's only app script. It reads content/articles.js and renders each page. */
(function () {
  'use strict';
  const articles = window.TECHSEEKHO_ARTICLES || [];
  const categories = ['AI & ChatGPT', 'AI Tools', 'Technology', 'Coding', 'Web Development', 'Android Apps', 'Laptops', 'Smartphones', 'Internet & Wi-Fi', 'Cyber Safety', 'Software', 'Tech Tips', 'Student Tech', 'How-To Guides', 'Gadgets'];
  const page = document.body.dataset.page;
  const baseUrl = 'https://your-username.github.io/techseekho/'; // Replace after publishing.
  const $ = (selector) => document.querySelector(selector);
  const formatDate = (date) => new Intl.DateTimeFormat('hi-IN', { day: 'numeric', month: 'short', year: 'numeric' }).format(new Date(`${date}T00:00:00`));
  const articleUrl = (article) => `article.html?slug=${encodeURIComponent(article.slug)}`;
  const escapeHtml = (value) => String(value).replace(/[&<>'"]/g, (character) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;' })[character]);

  function schema(data) { const script = document.createElement('script'); script.type = 'application/ld+json'; script.textContent = JSON.stringify(data); document.head.appendChild(script); }
  function addGlobalSchema() { schema({ '@context': 'https://schema.org', '@type': 'WebSite', name: 'TechSeekho', url: baseUrl, description: 'Hindi and Hinglish technology blog', potentialAction: { '@type': 'SearchAction', target: `${baseUrl}articles.html?q={search_term_string}`, 'query-input': 'required name=search_term_string' } }); }
  function header() {
    $('#site-header').innerHTML = `<div class="progress" id="reading-progress"></div><div class="site-header"><nav class="nav container" aria-label="Main navigation"><a class="brand" href="index.html" aria-label="TechSeekho home">Tech<span>Seekho</span></a><button class="menu-btn" aria-label="Menu kholein" aria-expanded="false">☰</button><div class="nav-links"><a href="index.html" class="${page === 'home' ? 'active' : ''}">Home</a><a href="articles.html?category=AI%20%26%20ChatGPT">AI</a><a href="articles.html?category=Technology">Technology</a><a href="articles.html?category=Coding">Coding</a><a href="articles.html?category=Laptops">Laptops</a><a href="articles.html?category=Android%20Apps">Apps</a><a href="articles.html?category=How-To%20Guides">How-To</a><a href="categories.html" class="${page === 'categories' ? 'active' : ''}">More</a></div><a class="search-icon" href="articles.html" aria-label="Search articles" title="Search">⌕</a><button class="icon-btn" id="theme-toggle" aria-label="Dark mode toggle" title="Theme badlein">◐</button></nav></div>`;
    const navLinks = $('.nav-links'); const menu = $('.menu-btn');
    menu.addEventListener('click', () => { const open = navLinks.classList.toggle('open'); menu.setAttribute('aria-expanded', open); });
    const dark = localStorage.getItem('techseekho-theme') === 'dark'; document.body.classList.toggle('dark', dark);
    $('#theme-toggle').addEventListener('click', () => { document.body.classList.toggle('dark'); localStorage.setItem('techseekho-theme', document.body.classList.contains('dark') ? 'dark' : 'light'); });
  }
  function footer() { $('#site-footer').innerHTML = `<footer class="site-footer"><div class="container footer-grid"><div><a class="brand" href="index.html">Tech<span>Seekho</span></a><p>Hindi/Hinglish mein calm, practical technology learning for curious students and everyday users.</p></div><div><strong>Popular Categories</strong><a href="articles.html?category=AI%20%26%20ChatGPT">AI & ChatGPT</a><a href="articles.html?category=Coding">Coding</a><a href="articles.html?category=Laptops">Laptops</a><a href="articles.html?category=Android%20Apps">Apps</a><a href="articles.html?category=Tech%20Tips">Tech Tips</a></div><div><strong>Important Links</strong><a href="about.html">About</a><a href="contact.html">Contact</a><a href="privacy.html">Privacy Policy</a><a href="disclaimer.html">Disclaimer</a><a href="terms.html">Terms</a><a href="affiliate.html">Affiliate Disclosure</a></div></div><div class="container copyright">© 2026 TechSeekho. All rights reserved.</div></footer>`; }
  function card(article) { return `<article class="article-card"><a href="${articleUrl(article)}" aria-label="${escapeHtml(article.title)} padhein"><img class="card-image" src="${article.image}" alt="${escapeHtml(article.title)}" loading="lazy" width="640" height="360"></a><div class="card-body"><div class="category">${escapeHtml(article.category)}</div><h3><a href="${articleUrl(article)}">${escapeHtml(article.title)}</a></h3><p>${escapeHtml(article.description)}</p><div class="meta"><span>${article.readingTime}</span><span>•</span><span>${article.views} demo views</span></div></div></article>`; }
  function ad() { return '<div class="ad" role="complementary" aria-label="Advertisement placeholder">Advertisement space - future ads or sponsored content will be clearly labelled here.</div>'; }
  function section(title, list, link = 'articles.html') { return `<section class="section"><div class="container"><div class="section-title"><h2>${title}</h2><a class="text-link" href="${link}">Sab dekhein →</a></div><div class="article-grid">${list.map(card).join('')}</div></div></section>`; }
  function home() { const featured = articles[10] || articles[0]; const latest = articles.slice().sort((a, b) => b.date.localeCompare(a.date)).slice(0, 6); const popular = articles.filter((article) => article.popular).slice(0, 3); const trending = articles.filter((article) => article.trending || article.popular).slice(0, 8); const byCat = (category) => articles.filter((article) => article.category === category).slice(0, 3); $('#main-content').innerHTML = `<section class="hero"><div class="container hero-grid"><div><div class="eyebrow">Simple tech, smarter choices</div><h1>Technology ko aasan bhasha mein seekho.</h1><p>AI tools, coding, apps aur gadgets par practical Hindi/Hinglish guides. Seedhi baat, useful advice.</p><form class="search" action="articles.html" role="search"><label class="sr-only" for="home-search">Articles search karein</label><input id="home-search" name="q" placeholder="AI tools, laptop, coding..." required><button>Search</button></form></div><img class="hero-image" src="${featured.image}" alt="Modern technology workspace" width="720" height="405"></div></section><section class="section trend-section"><div class="container"><div class="section-title"><h2>Trending now</h2><span class="demo-label">Evergreen guides</span></div><ol class="trending-list">${trending.map((a, i) => `<li><span class="rank">${String(i + 1).padStart(2, '0')}</span><a href="${articleUrl(a)}"><strong>${escapeHtml(a.title)}</strong><small>${a.category} · ${a.readingTime}</small></a></li>`).join('')}</ol></div></section><section class="section"><div class="container"><div class="section-title"><h2>Featured guide</h2></div><article class="featured"><img src="${featured.image}" alt="${escapeHtml(featured.title)}" width="760" height="430"><div class="featured-content"><div class="category">${featured.category}</div><h2>${escapeHtml(featured.title)}</h2><p>${escapeHtml(featured.description)}</p><div class="meta"><span>${formatDate(featured.date)}</span><span>•</span><span>${featured.readingTime}</span></div><p><a class="text-link" href="${articleUrl(featured)}">Poora article padhein →</a></p></div></article>${ad()}</div></section>${section('Latest articles', latest)}<section class="section category-band"><div class="container"><div class="section-title"><h2>Category explorer</h2><a class="text-link" href="categories.html">All categories →</a></div><div class="categories">${categories.map((category) => `<a class="category-link" href="articles.html?category=${encodeURIComponent(category)}">${category}<small>${articles.filter(a => a.category === category).length} guides</small></a>`).join('')}</div></div></section>${section('AI, explained simply', byCat('AI & ChatGPT').concat(byCat('AI Tools')).slice(0, 3), 'articles.html?category=AI%20%26%20ChatGPT')}${section('Coding & Web Development', byCat('Coding').concat(byCat('Web Development')).slice(0, 3), 'articles.html?category=Coding')}${section('Laptop & Gadget guides', byCat('Laptops').concat(byCat('Gadgets')).slice(0, 3), 'articles.html?category=Laptops')}${section('Student Tech', byCat('Student Tech'), 'articles.html?category=Student%20Tech')}${section('Popular articles', popular)}${section('How-To guides', articles.filter(a => a.category === 'How-To Guides' || a.title.includes('kaise')).slice(0, 3))}${newsletter()}`; }
  function newsletter() { return `<section class="newsletter"><div class="container newsletter-grid"><div><h2>Har week tech ko thoda aur simple banayein.</h2><p>Newsletter integration ke liye ready UI. Publish se pehle service connect karein.</p></div><form id="newsletter-form"><label class="sr-only" for="newsletter-email">Email address</label><input id="newsletter-email" type="email" placeholder="aap@email.com" required><button>Subscribe</button></form></div></section>`; }
  function articlesPage() { const params = new URLSearchParams(location.search); let active = params.get('category') || 'All'; const q = (params.get('q') || '').trim(); const render = () => { const needle = ($('#article-search').value || '').toLowerCase(); const list = articles.filter((article) => (active === 'All' || article.category === active) && (!needle || [article.title, article.category, article.description, article.tags.join(' '), article.intro || '', ...(article.content || []).flat()].join(' ').toLowerCase().includes(needle))); $('#article-results').innerHTML = list.length ? list.map(card).join('') : '<p class="empty">Is search ke liye koi article nahi mila. Dusra keyword try karein.</p>'; $('#result-count').textContent = `${list.length} result${list.length === 1 ? '' : 's'} found`; document.querySelectorAll('.filter').forEach((button) => button.classList.toggle('active', button.dataset.category === active)); };
    $('#main-content').innerHTML = `<section class="container page-head"><h1>All Articles</h1><p>Practical technology guides, ek-ek step karke.</p></section><section class="container section" style="padding-top:10px"><label class="sr-only" for="article-search">Articles search karein</label><input id="article-search" class="search-input" value="${escapeHtml(q)}" placeholder="Title, category, tags, description ya content search karein" style="width:100%;padding:13px;border:1px solid var(--line);border-radius:6px;background:var(--surface);color:var(--text);margin-bottom:12px"><p id="result-count" class="results-count"></p><div class="filter-row">${['All', ...categories].map((category) => `<button class="filter ${category === active ? 'active' : ''}" data-category="${category}">${category}</button>`).join('')}</div><div id="article-results" class="article-grid"></div></section>`;
    $('#article-search').addEventListener('input', render); document.querySelectorAll('.filter').forEach((button) => button.addEventListener('click', () => { active = button.dataset.category; render(); })); render(); }
  function articlePage() { const slug = new URLSearchParams(location.search).get('slug'); const article = articles.find((item) => item.slug === slug) || articles[0]; const canonical = `${baseUrl}article.html?slug=${encodeURIComponent(article.slug)}`; document.title = `${article.title} | TechSeekho`; $('meta[name="description"]').setAttribute('content', article.description); $('#canonical-link').href = canonical; $('#og-title').content = `${article.title} | TechSeekho`; $('#og-description').content = article.description; $('#og-url').content = canonical;
    schema({ '@context': 'https://schema.org', '@type': 'BlogPosting', headline: article.title, description: article.description, image: article.image, datePublished: article.date, author: { '@type': 'Person', name: article.author }, publisher: { '@type': 'Organization', name: 'TechSeekho' }, mainEntityOfPage: canonical }); schema({ '@context': 'https://schema.org', '@type': 'BreadcrumbList', itemListElement: [{ '@type': 'ListItem', position: 1, name: 'Home', item: baseUrl }, { '@type': 'ListItem', position: 2, name: 'Articles', item: `${baseUrl}articles.html` }, { '@type': 'ListItem', position: 3, name: article.title, item: canonical }] });
    const related = articles.filter((item) => item.category === article.category && item.slug !== article.slug).slice(0, 3); const current = articles.indexOf(article); const previous = articles[current - 1]; const next = articles[current + 1]; const toc = article.content.map(([heading], i) => `<li><a href="#section-${i + 1}">${escapeHtml(heading)}</a></li>`).join(''); $('#main-content').innerHTML = `<div class="container article-layout"><article><nav class="meta" aria-label="Breadcrumb"><a href="index.html">Home</a><span>/</span><a href="articles.html">Articles</a><span>/</span><a href="articles.html?category=${encodeURIComponent(article.category)}">${escapeHtml(article.category)}</a></nav><div class="category" style="margin-top:18px">${escapeHtml(article.category)}</div><h1 class="article-title">${escapeHtml(article.title)}</h1><p class="article-lead">${escapeHtml(article.intro)}</p><div class="meta"><span>${formatDate(article.date)}</span><span>•</span><span>By ${escapeHtml(article.author)}</span><span>•</span><span>${article.readingTime}</span></div><img class="article-cover" src="${article.image}" alt="${escapeHtml(article.title)}" width="1200" height="600"><div class="article-toc"><strong>In this guide</strong><ol>${toc}</ol></div><div class="article-body">${article.content.map(([heading, paragraph], i) => `<section id="section-${i + 1}"><h2>${escapeHtml(heading)}</h2><p>${escapeHtml(paragraph)}</p></section>`).join('')}<aside class="important-box"><strong>Important note</strong><p>${escapeHtml(article.importantNote)}</p></aside><section><h2>Frequently asked questions</h2>${article.faq.map(([question, answer]) => `<details><summary>${escapeHtml(question)}</summary><p>${escapeHtml(answer)}</p></details>`).join('')}</section><section><h2>Conclusion</h2><p>Small, informed steps technology ko less confusing banate hain. Is guide ko bookmark karein aur ek practical step aaj try karein.</p></section></div><div class="share-row"><strong>Share this guide:</strong><button data-share="copy" type="button">Copy link</button><a href="https://wa.me/?text=${encodeURIComponent(article.title + ' ' + canonical)}" target="_blank" rel="noopener">WhatsApp</a></div><div class="tag-list" aria-label="Tags">${article.tags.map((tag) => `<a class="tag" href="articles.html?q=${encodeURIComponent(tag)}">#${escapeHtml(tag)}</a>`).join('')}</div><nav class="post-nav" aria-label="Article navigation">${previous ? `<a href="${articleUrl(previous)}">← Previous<br><strong>${escapeHtml(previous.title)}</strong></a>` : '<span></span>'}${next ? `<a href="${articleUrl(next)}">Next →<br><strong>${escapeHtml(next.title)}</strong></a>` : ''}</nav></article><aside><div class="side-note"><strong>TechSeekho note</strong><br>Is guide ko apni needs ke hisaab se use karein. Product details aur policies source par confirm karein.</div></aside></div><section class="section" style="padding-top:0"><div class="container"><div class="section-title"><h2>Related articles</h2></div><div class="article-grid">${related.map(card).join('') || '<p class="empty">Aur related guides jaldi aayenge.</p>'}</div></div></section><button class="back-top" id="back-top" aria-label="Back to top">↑</button>`; $('#back-top').addEventListener('click', () => window.scrollTo({ top: 0, behavior: 'smooth' })); document.querySelector('[data-share="copy"]').addEventListener('click', () => { navigator.clipboard?.writeText(canonical); document.querySelector('[data-share="copy"]').textContent = 'Link copied'; }); }
  function categoriesPage() { $('#main-content').innerHTML = `<section class="container page-head"><h1>Categories</h1><p>Apne interest ka topic choose karein.</p></section><section class="container section" style="padding-top:10px"><div class="categories">${categories.map((category) => { const count = articles.filter((a) => a.category === category).length; return `<a class="category-link" href="articles.html?category=${encodeURIComponent(category)}">${category}<br><small>${count} articles</small></a>`; }).join('')}</div></section>`; }
  function prosePage(title, intro, body) { $('#main-content').innerHTML = `<section class="container prose"><h1>${title}</h1><p class="notice">${intro}</p>${body}</section>`; }
  function staticPage() { if (page === 'about') prosePage('About TechSeekho', 'Technology ko samajhna difficult nahi hona chahiye.', '<h2>What we cover</h2><p>AI, coding, web development, apps, laptops, cyber safety aur student tech: har topic ko simple Hindi/Hinglish mein explain kiya jata hai.</p><h2>Who this is for</h2><p>Students, first-time buyers aur everyday users jo jargon ke bina better digital choices lena chahte hain.</p><h2>Our content philosophy</h2><p>Hum clear language, useful context, safe practical actions aur honest limitations ko priority dete hain. Technology change hoti rehti hai, isliye important decisions se pehle official sources check karein.</p>'); if (page === 'contact') $('#main-content').innerHTML = `<section class="container prose"><h1>Contact</h1><p>Suggestion, correction ya collaboration idea share karein.</p><p class="notice">Yeh static demo form hai. Isse email nahi bheja jayega jab tak aap Formspree, Netlify Forms ya doosri form service connect nahi karte.</p><form class="contact-form" id="contact-form"><label>Name<input required name="name" autocomplete="name"></label><label>Email<input required type="email" name="email" autocomplete="email"></label><label>Message<textarea required name="message" rows="6"></textarea></label><button>Message ready karein</button></form><p id="form-status" role="status"></p></section>`; if (page === 'privacy') prosePage('Privacy Policy', 'Last updated: 5 September 2026. Customize this page before commercial use.', '<h2>Information</h2><p>TechSeekho is static website mein visitor accounts ya personal data collect nahi karta. Agar aap future mein newsletter ya contact form service connect karte hain, us service ki privacy policy apply hogi.</p><h2>Cookies and third parties</h2><p>Current version mein analytics aur advertising trackers enabled nahi hain. Future integrations ko is policy mein clearly update kiya jayega.</p>'); if (page === 'disclaimer') prosePage('Disclaimer', 'Customize this page before commercial use.', '<h2>Not professional advice</h2><p>Content financial, legal, medical, security ya professional advice nahi hai. Important decisions ke liye official documentation aur qualified professional se confirm karein.</p><h2>External links</h2><p>Future mein external resources link ho sakte hain. Hum un websites ke content ya policies control nahi karte.</p>'); if (page === 'terms') prosePage('Terms & Conditions', 'Customize this page before commercial use.', '<h2>Use of content</h2><p>Articles personal learning ke liye hain. Content copy, republish ya redistribute karne se pehle written permission lein.</p><h2>Changes</h2><p>Website aur terms time ke saath update ho sakte hain. Continued use ka matlab updated terms ko accept karna hai.</p>'); if (page === 'affiliate') prosePage('Affiliate Disclosure', 'Customize this page before using affiliate links commercially.', '<h2>Our approach</h2><p>TechSeekho currently does not publish affiliate links. If that changes, qualifying links will be clearly identified near the recommendation.</p><h2>Editorial independence</h2><p>Recommendations should be based on usefulness and fit, not commission. Prices, availability and terms must always be verified on the seller website.</p>'); }
  header(); footer(); addGlobalSchema(); if (page === 'home') home(); if (page === 'articles') articlesPage(); if (page === 'article') articlePage(); if (page === 'categories') categoriesPage(); if (['about', 'contact', 'privacy', 'disclaimer', 'terms', 'affiliate'].includes(page)) staticPage(); window.addEventListener('scroll', () => { const max = document.documentElement.scrollHeight - innerHeight; $('#reading-progress').style.width = `${max ? (scrollY / max) * 100 : 0}%`; });
  document.addEventListener('submit', (event) => { if (event.target.id === 'newsletter-form' || event.target.id === 'contact-form') { event.preventDefault(); const status = $('#form-status'); if (status) status.textContent = 'Thank you. Form service connect hone ke baad messages receive honge.'; else alert('Newsletter service connect hone ke baad subscription active hoga.'); } });
}());
